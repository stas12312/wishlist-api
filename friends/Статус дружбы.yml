info:
  name: Статус дружбы
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/friends/:user_id/status"
  params:
    - name: user_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
