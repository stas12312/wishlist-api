info:
  name: Список друзей пользователя
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/api/users/:username/friends"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
