info:
  name: Счетчик друзей пользователей
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/api/users/:username/friends/info"
  params:
    - name: username
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
