info:
  name: Список заросов
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/friends/requests/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
