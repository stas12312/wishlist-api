info:
  name: Удалить из друзей
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/friends/delete"
  body:
    type: json
    data: |-
      {
        "user_id": {{user_id}}
      }
  auth: inherit

runtime:
  variables:
    - name: user_id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
