info:
  name: Получение статьи
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/admin/articles/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
