info:
  name: Удалить статью
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/admin/articles/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
