info:
  name: Список категорий
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/admin/tickets/categories"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
