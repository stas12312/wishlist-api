info:
  name: Получить все обращения
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/admin/tickets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
