info:
  name: Получение сообщений тикета
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/admin/tickets/:id/conversation"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
