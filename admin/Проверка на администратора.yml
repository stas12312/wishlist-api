info:
  name: Проверка на администратора
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/admin/is"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
