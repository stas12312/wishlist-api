info:
  name: Список статей
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/articles/?count=10&cursor"
  params:
    - name: count
      value: "10"
      type: query
    - name: cursor
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
