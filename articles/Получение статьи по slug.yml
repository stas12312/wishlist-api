info:
  name: Получение статьи по slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/articles/:slug"
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
