info:
  name: Список забронированных желаний
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/api/wishes/reserved"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
