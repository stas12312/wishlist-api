info:
  name: Получение желания
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/wishes/:uuid"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
