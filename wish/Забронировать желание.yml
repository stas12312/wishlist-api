info:
  name: Забронировать желание
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/api/wishes/:uuid/reserve"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
