info:
  name: Отменить бронирование желания
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/api/wishes/:uuid/cancel_reserve"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
