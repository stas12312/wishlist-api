info:
  name: Список желаний
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/wishlists/:uuid/wishes"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
