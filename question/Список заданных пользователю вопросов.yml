info:
  name: Список заданных пользователю вопросов
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/questions/for-me?count=20"
  params:
    - name: count
      value: "20"
      type: query
    - name: cursor
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
