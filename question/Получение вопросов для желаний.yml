info:
  name: Получение вопросов для желаний
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/wishes/:wish_uuid/questions"
  params:
    - name: wish_uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
