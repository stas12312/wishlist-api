info:
  name: Получить счетчики
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/questions/counters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
