info:
  name: Удалить вопрос
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/api/questions/:question_id"
  params:
    - name: question_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
