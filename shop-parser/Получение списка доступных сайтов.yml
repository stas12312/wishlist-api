info:
  name: Получение списка доступных сайтов
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/parse/available"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
