info:
  name: Получение статуса обработки
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/parse/:task_id/status"
  params:
    - name: task_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
