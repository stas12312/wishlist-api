info:
  name: Список категорий
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/tickets/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
