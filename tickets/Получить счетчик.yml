info:
  name: Получить счетчик
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/tickets/counters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
