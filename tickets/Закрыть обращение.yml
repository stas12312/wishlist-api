info:
  name: Закрыть обращение
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/tickets/:id/close"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
