info:
  name: Восстановление вишлиста
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/wishlists/:uuid/restore"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

runtime:
  variables:
    - name: user_id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
