info:
  name: Получение вишлиста
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/wishlists/:uuid"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
